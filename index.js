import { css, data, attr, classes, node } from './lib/core'
import { measure, mutate, removeMeasure, removeMutate } from './lib/batch'

export {
  css,
  data,
  attr,
  classes,
  node,
  node as el,
  measure,
  mutate,
  removeMeasure,
  removeMutate
}

export default {
  css, data, attr, classes, node
}
